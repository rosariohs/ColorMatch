<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rheredias.colormatch">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/color_match_logo"
        android:label="Color Match"
        android:roundIcon="@drawable/color_match_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Colormatch">
        <activity
            android:name="com.rheredias.colormatch.ui.MainActivity"
            android:label=""
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>